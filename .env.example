# AI Digest Environment Variables

# === REQUIRED CORE CONFIGURATION ===

# Gmail OAuth Configuration (Required)
GMAIL_CLIENT_ID=your-gmail-client-id
GMAIL_CLIENT_SECRET=your-gmail-client-secret
GMAIL_REFRESH_TOKEN=your-gmail-refresh-token

# OpenAI Configuration (Required)
OPENAI_API_KEY=your-openai-api-key

# Email Service (Required)
RESEND_API_KEY=your-resend-api-key
RECIPIENT_EMAIL=your-email@example.com

# AWS Configuration (Required for AWS deployment)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=your-aws-account-id

# === OPTIONAL ENHANCEMENTS ===

# Advanced AI Models (Optional - defaults to gpt-4o-mini)
CLASSIFICATION_MODEL=gpt-4o-mini  # Fast model for email classification
SUMMARIZATION_MODEL=gpt-4o         # Main summarization model
ANALYSIS_MODEL=gpt-o1              # Deep analysis model (when available)

# Content Extraction (Optional - enables deep article extraction)
FIRECRAWL_API_KEY=your-firecrawl-api-key

# Web Search (Optional - enables additional research)
BRAVE_API_KEY=your-brave-search-api-key

# Monitoring (Optional)
HELICONE_API_KEY=your-helicone-api-key

# === AWS-SPECIFIC CONFIGURATION ===

# DynamoDB Tables (Auto-created by Terraform)
DYNAMODB_TABLE=ai-digest-known-ai-senders
DYNAMODB_NON_AI_TABLE=ai-digest-known-non-ai-senders

# S3 Configuration (Auto-created by Terraform)
S3_BUCKET=ai-digest-processed-emails-{account-id}-{region}
STORAGE_TYPE=s3  # or 'dynamodb'

# Lambda Function Names (Auto-created by Terraform)
WEEKLY_DIGEST_FUNCTION_NAME=ai-digest-weekly-digest
RUN_NOW_FUNCTION_NAME=ai-digest-run-now

# === OPTIONAL ADVANCED SETTINGS ===

# Cost Control (Optional)
MAX_COST_PER_RUN=10                # Maximum dollars to spend per run
MAX_OPENAI_CALLS_PER_RUN=50        # Maximum OpenAI API calls
MAX_EMAILS_PER_RUN=500             # Maximum emails to process

# Feature Flags (Optional)
ENABLE_FIRECRAWL=true              # Use Firecrawl for article extraction
ENABLE_BRAVE_SEARCH=true           # Use Brave for additional research
ENABLE_CRITIC_AGENT=true           # Generate opinionated commentary

# Professional Context (Optional)
PROFESSIONS=Software Engineer,ML Engineer,Data Scientist,Product Manager,Founder
PRODUCT_CONTEXT='{"apps":[]}'      # JSON array of your apps for personalized insights

# Debug/Development (Optional)
LOG_LEVEL=info                     # debug, info, warn, error
NODE_ENV=production                # production, development